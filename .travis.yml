language: python
python:
  - "3.6"
env:
  global:
    - LOCAL_DATA_FILE=../validation_data/Hadoop_2k.json
    - LOCAL_RESULTS_FILE=/tmp/local-results.txt
install:
  - "pip install pipenv"
  - "pipenv install"
  - "pipenv install --dev"
script:
  - "pipenv graph"
  - "pipenv run pytest"
